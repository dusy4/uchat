<ContentDialog
    x:Class="uchat.CreateGroupDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:uchat.Models"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="Create New Group"
    PrimaryButtonText="Create"
    SecondaryButtonText="Cancel"
    DefaultButton="Primary">

    <StackPanel Spacing="15" MinWidth="350">
        <TextBox x:Name="GroupNameBox" Header="Group Name" PlaceholderText="Enter group name..."/>

        <TextBlock Text="Select Members:" FontWeight="SemiBold"/>
        <ListView x:Name="MembersList" SelectionMode="Multiple" Height="200" BorderBrush="{ThemeResource SurfaceStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="4">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="local:UserItemModel">
                    <StackPanel Orientation="Horizontal" Spacing="10" Padding="5">
                        <Grid Width="32" Height="32" CornerRadius="16">
                            <Ellipse Fill="{Binding AvatarColor}"/>
                            <TextBlock Text="{Binding Initials}" VerticalAlignment="Center" HorizontalAlignment="Center" Foreground="White" FontSize="12" FontWeight="Bold"/>
                            <Image Source="{Binding ProfileImage}" Stretch="UniformToFill"/>
                        </Grid>
                        <TextBlock Text="{Binding Username}" VerticalAlignment="Center"/>
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </StackPanel>
</ContentDialog>